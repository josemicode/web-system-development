# REST API for books

Please create a .env file with the following variables:
- PORT

here's a quick command you can run:
```
echo "PORT=3000" > .env
```

If you don't, no problem, you can run "start"; but if you run dev, you'll get an error, as the env file is required.